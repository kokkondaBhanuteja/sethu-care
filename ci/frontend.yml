# Copy to .github/workflows/frontend.yml
name: frontend

# Always runs (no `paths:` filter) so the REQUIRED "frontend" check always reports instead of hanging
# on GitHub as "Expected" on backend-only PRs. The first step detects frontend changes; the heavy
# steps skip when there are none, and the job still completes green in seconds.
on:
  push:
    branches: [developer, main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0 # full history so change detection can diff against the base

      # Self-contained change detection (no third-party action → no Node-runtime deprecation).
      # working-directory: . so git runs at the repo root, not in frontend/.
      - name: Detect frontend changes
        id: changes
        working-directory: .
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
            head="${{ github.event.pull_request.head.sha }}"
          else
            base="${{ github.event.before }}"
            head="${{ github.sha }}"
          fi
          if ! git cat-file -e "$base" 2>/dev/null; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"; exit 0
          fi
          if git diff --name-only "$base" "$head" \
            | grep -qE '(^frontend/|^backend/api/openapi\.yaml$|^\.github/workflows/frontend\.yml$)'; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
          else
            echo "frontend=false" >> "$GITHUB_OUTPUT"
          fi

      - name: No frontend changes — reporting success
        if: steps.changes.outputs.frontend != 'true'
        working-directory: .
        run: echo "No frontend files changed; skipping frontend checks."

      - uses: pnpm/action-setup@v4
        if: steps.changes.outputs.frontend == 'true'
        with:
          version: 10

      - uses: actions/setup-node@v5
        if: steps.changes.outputs.frontend == 'true'
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install
        if: steps.changes.outputs.frontend == 'true'
        run: pnpm install --frozen-lockfile

      - name: Generated API client is up to date (drift guard)
        if: steps.changes.outputs.frontend == 'true'
        run: |
          pnpm --filter @sethu/api-client run generate
          git diff --exit-code packages/api-client/src/generated \
            || (echo "::error::packages/api-client is stale — run 'pnpm api:generate' and commit." && exit 1)

      - name: Typecheck
        if: steps.changes.outputs.frontend == 'true'
        run: pnpm typecheck

      - name: Lint
        if: steps.changes.outputs.frontend == 'true'
        run: pnpm lint

      - name: Format check
        if: steps.changes.outputs.frontend == 'true'
        run: pnpm format:check
