# Dependencies
node_modules/
.pnpm-store/

# Build output / caches
dist/
.turbo/
.next/
out/

# Native (Capacitor ios/ + android/ are COMMITTED source; only their derived
# artifacts are ignored — the per-project .gitignores inside them handle Pods/build)
DerivedData/

# Env / secrets
.env
.env.*
!.env.example

# Logs / OS
*.log
.DS_Store

# Ad-hoc investigation scripts. Never committed.
_*.mjs
d_*.txt
