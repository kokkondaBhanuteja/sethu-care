import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "in.sethucare.provider",
  appName: "SETHU-CARE Partner",
  webDir: "dist",
};

export default config;
